# משימות אבטחה — vela

מסמך-עבודה למי שמתקן. כל משימה עצמאית: מה שבור, איפה, מה לשנות, ואיך לבדוק שזה עבד.

**מקור:** סריקת-אבטחה מלאה, 53 ממצאים שהועלו ו-48 שאושרו מול הקוד האמיתי.
כאן הם מאוחדים ל-30 משימות ייחודיות (ממצא כמו "עוגייה בלי `Secure`" הופיע מארבעה
כיוונים שונים — זו משימה אחת).

---

## חוקי-ברזל למי שמתקן

1. **אין תלויות חדשות.** הפרויקט רץ על `node` נקי בלי `npm install`. אין express, אין
   helmet, אין bcrypt. כל תיקון חייב להשתמש במודולים המובנים של Node (`crypto`, `http`,
   `fs`, `path`) בלבד.
2. **הערות בעברית.** כל הקוד מוער בעברית. תיקון עם הערות באנגלית לא מתאים לקוד הקיים.
3. **בודקים אחרי כל משימה.** אחרי כל שינוי: `node --check <קובץ>`, ואז מריצים
   `node server.js` ומוודאים שהאתר עולה ושהדפים מחזירים 200. פירוט בכל משימה.
4. **לא שוברים את האפליקציה.** לפני שמצמצמים משהו (תקרת-גודל, רשימת-היתר) בודקים מה
   האפליקציה באמת צריכה. משימה שסוגרת חור וגם מפילה את האתר היא כישלון.
5. **משימה אחת בכל פעם, קומיט אחד לכל משימה.** מזהה המשימה בכותרת הקומיט.
6. **אין `console.log` של סודות.** לא מדפיסים מפתחות, טוקנים או סיסמאות — גם לא בדיבאג.

### רקע על המערכת

שרת `http` של Node בלי framework. כל נתיב הוא ענף `if (url.startsWith(...))` בתוך
`http.createServer` ב-`server.js`. שלוש מערכות-session נפרדות:

| עוגייה | למי | מודול |
|---|---|---|
| `sid` | תלמיד | `lib/session.js` |
| `asid` | בעלים/אדמין | `lib/admin-auth.js` |
| `psid` | הורה | `lib/parent-auth.js` |

המשתמשים הם **ילדים בני 6–12**. הנתונים כוללים שם, גיל, כיתה, בית-ספר, כל תרגיל
שפתרו, ומה שהמודל כתב עליהם. דליפה כאן היא תיק אישי על קטין מזוהה.

---

## ✅ 0 · כבר תוקן — לא לגעת

**הגשת קבצים סטטית ללא רשימת-היתר** (`server.js`, הענף האחרון בנתב).
כל משתמש מחובר יכול היה למשוך כל קובץ בפרויקט — `data/users.json`, `data/sessions.json`,
`data/admin-sessions.json`, `.env`, `lib/*.js`. תוקן בקומיט `8c8c932`: נוספה
`isClientAsset()` שמאשרת רק סיומות של נכסי-לקוח, ורק בתיקיות `fonts/ images/ golden/
teacher-character/` או ברמת השורש.

**אם משנים את הפונקציה הזו — לבדוק מחדש:**
```bash
# חייב להחזיר 404 (עם עוגיית sid תקפה):
for p in /.env /data/users.json /data/sessions.json /lib/llm.js /server.js; do
  curl -s -o /dev/null -w "$p %{http_code}\n" -H "cookie: sid=$SID" "http://localhost:8787$p"
done
# חייב להחזיר 200:
for p in /app.js /kids-theme.css /favicon-v.png /teacher-character/faces/happy.png; do
  curl -s -o /dev/null -w "$p %{http_code}\n" -H "cookie: sid=$SID" "http://localhost:8787$p"
done
```

---

# קריטי

## S-01 · אין הגבלת-קצב על `/api/login`

**קבצים:** `server.js` (הענף `/api/login`), `lib/users.js`, קובץ חדש `lib/throttle.js`
**זמן:** שעות

**מה שבור.** הענף `/api/login` קורא ל-`users.authenticate` ישירות. אין מונה-כישלונות,
אין נעילה, אין השהיה, אין מעקב לפי IP. נמדד בפועל: **30 ניסיונות שגויים ב-973
מילישניות**, וכל 30 החזירו 401 — והסיסמה הנכונה עבדה מיד אחריהם. סיסמת מינימום היא
6 תווים (`lib/users.js`), ו-4 כשהבעלים קובע סיסמה.

`lib/admin-auth.js` ו-`lib/parent-auth.js` **כן** מגנים על עצמם עם מונה ונעילה — רק
מסלול התלמיד חשוף. וחשוב: אזור ההורים מתחבר עם **אותם פרטים** של הילד, כך שסיסמת-ילד
שנפרצת פותחת גם את תיק-ההורים (חוות-דעת פסיכולוגית, דפוסי-למידה).

**מה לעשות.** ליצור `lib/throttle.js` משותף ולחבר אותו לשלושת מסלולי-ההתחברות:

- מפתח כפול: גם שם-המשתמש המנורמל וגם ה-IP של הלקוח
  (`req.headers["x-forwarded-for"]` ראשון, אחרת `req.socket.remoteAddress` — ב-Render
  יש proxy, אז ה-header הוא המקור).
- השהיה מדורגת אחרי 5 כישלונות, נעילה קשה אחרי 15 בתוך 15 דקות.
- לשמור את המונים ב-`data/login-throttle.json` דרך `lib/store.js`, כדי שהפעלה-מחדש
  של Render לא תאפס אותם.
- להעלות מינימום-סיסמה ל-8 תווים ב-`lib/users.js`, ולדחות רשימת סיסמאות נפוצות.

**בונוס באותו קובץ:** `crypto.scryptSync` ב-`lib/users.js` הוא **סינכרוני** ולוקח ~24ms.
כ-40 בקשות-התחברות במקביל מקפיאות את הלולאה לכל הילדים באתר. להחליף ל-`crypto.scrypt`
האסינכרוני (עטוף ב-Promise).

**בדיקה:**
```bash
# 20 ניסיונות שגויים — האחרונים חייבים להיחסם (429), לא 401
for i in $(seq 1 20); do
  curl -s -o /dev/null -w "%{http_code} " -X POST -H 'content-type: application/json' \
    -d '{"username":"אורי","password":"wrong'"$i"'"}' http://localhost:8787/api/login
done; echo
```

---

## S-02 · מפתח Anthropic ישן בהיסטוריית git של מאגר ציבורי

**קובץ:** היסטוריית git — קומיט `b4ffce7`
**זמן:** דקות (הפעולה עצמה) + החלטה על השאר

**מה שבור.** `.env` נכנס ל-`.gitignore` רק **אחרי** שכבר נדחף. הקומיט `b4ffce7`
("Add files via upload") מכיל `.env` עם `ANTHROPIC_API_KEY`. המאגר
`github.com/OriSharon2308/-` הוא **ציבורי** — אומת דרך ה-API של GitHub
(`"visibility": "public"`).

המפתח כבר הוחלף ב-`.env` המקומי (חתימות שונות), אבל **החלפה אינה ביטול**: אם המפתח
הישן לא בוטל בקונסולת Anthropic, הוא עדיין עובד וכל חיוב עליו הוא של הבעלים.

**מה לעשות.**
1. **קודם כול — לבטל את המפתח הישן בקונסולת Anthropic.** זו הפעולה היחידה שבאמת
   סוגרת את זה. מחיקה מהיסטוריית git לא עוזרת אם המפתח כבר נשמר במקום אחר.
2. רק אחר כך, אם רוצים לנקות את ההיסטוריה: `git filter-repo --path .env --invert-paths`
   ואז `git push --force`. **זה כותב מחדש את כל ההיסטוריה** — לוודא שאין עוד clone פעיל.
3. להחליט אם המאגר צריך להישאר ציבורי. אם כן — מודל-האבטחה חייב להניח שכל שורת קוד
   גלויה לתוקף, וזה מגביר את הדחיפות של כל שאר המשימות כאן.

**בדיקה:**
```bash
git log --all -S'sk-ant' --oneline    # חייב לחזור ריק אחרי הניקוי
```

---

## S-03 · טוקני-session נשמרים גולמיים — הקובץ עצמו הוא הסיסמה

**קבצים:** `lib/session.js`, `lib/admin-auth.js`, `lib/parent-auth.js`, `lib/store.js`
**זמן:** שעות

**מה שבור.** שלושת מודולי ה-session שומרים את הטוקן כ**מפתח גולמי** ב-JSON:
`{"c9a5cea0d76b4f...": {"createdAt": ...}}`. פונקציות האימות עושות חיפוש-מפתח ישיר,
בלי קשירה ל-IP או ל-user-agent. המשמעות: מי שמשיג את הקובץ מקבל גישה מיידית לכל חשבון.

הוכח בסריקה: הוצאת הטוקן מ-`admin-sessions.json`, הדבקתו כעוגיית `asid`, ו-
`/api/admin/users` החזיר את רשימת כל הילדים.

בנוסף `lib/store.js` משכפל כל כתיבה ל-Upstash דרך `cloudStore.queueSync`, ו-
`lib/cloud-store.js` מסנן רק `logs/` — כלומר הטוקנים הגולמיים משוכפלים גם לשם.

> **הערה:** החור שאיפשר להוריד את הקבצים האלה מהרשת כבר נסגר (משימה 0). המשימה הזו
> היא שכבת-ההגנה השנייה: גם אם קובץ ידלוף שוב, הוא לא יהיה שווה כלום.

**מה לעשות.**
- לשמור `sha256(token)` כמפתח במקום הטוקן עצמו. באימות: לגבב את העוגייה ולחפש את הגיבוב.
  הטוקן הגולמי חוזר ללקוח פעם אחת בלבד, בעוגייה, ואינו נשמר בשום מקום.
- להוסיף `lastSeenAt` ולפוג sessions שלא היו בשימוש 30 יום.
- להחריג את קובצי ה-sessions מהשכפול ל-Upstash ב-`lib/cloud-store.js`, או לשכפל רק
  את הגיבובים.

**⚠️ פעולה תפעולית לפני התיקון:** למחוק את `data/sessions.json`,
`data/admin-sessions.json` ו-`data/parent-sessions.json` הקיימים. הם היו קריאים
מהאינטרנט, ולכן כל טוקן בהם צריך להיחשב כמי שנחשף. זה מנתק את כולם ומחייב כניסה מחדש.

**בדיקה:** אחרי התיקון — לפתוח את `data/sessions.json` ולוודא שאף מפתח בו אינו זהה
לערך שבעוגיית הדפדפן.

---

## S-04 · `/api/teach` — היסטוריה מהלקוח בלי תקרה, כפול לולאת-כלים של 8 סבבים

**קבצים:** `server.js` (ענף `/api/teach`), `agent/draw-agent.js`
**זמן:** שעות

**מה שבור.** `history` מגיע מהלקוח ועובר כמו-שהוא ל-`teacherDraw`. ב-
`agent/draw-agent.js` הוא נחתך ל-12 ההודעות האחרונות, אבל **כל הודעה עוברת
`String(m.content)` בלי שום חיתוך-אורך**. אין תקרת-תווים להודעה בודדת. במקביל
רצה לולאת tool-use של עד 8 סבבים, כך שכל בקשה מכפילה את העלות פי 8.

תוקף שולח 12 הודעות של מגה-בייט כל אחת, ומשלם עליהן הבעלים.

**מה לעשות.**
- לחתוך כל הודעה ל-~2000 תווים לפני שהיא נכנסת ל-prompt.
- לתקרר את סך ההיסטוריה (למשל 20,000 תווים) ולא רק את מספר ההודעות.
- להוריד את תקרת ה-`readJsonBody` בענף הזה לגודל סביר (למשל 256KB).
- לצרף את ההגבלה של **S-05** (תקציב per-user) שתופסת גם את מי שעוקף.

**בדיקה:** לשלוח `history` עם הודעה של 500KB ולוודא שהבקשה נדחית או נחתכת, ושמספר
הטוקנים שנרשם ב-`lib/token-usage.js` נשאר סביר.

---

# גבוה

## S-05 · `lib/token-usage.js` רק רושם הוצאה — אף פעם לא עוצר אותה

**קבצים:** `lib/token-usage.js`, כל הענפים ב-`server.js` שקוראים למודל
**זמן:** יום

**מה שבור.** המודול סופר טוקנים ודולרים לכל תלמיד, אבל אף אחד לא בודק את המספר לפני
הקריאה הבאה. אין תקרה יומית, אין מפסק. בשילוב עם הרשמה חופשית (S-06) זה מפעל-הוצאות.

**מה לעשות.** להוסיף `checkBudget(userId)` שנקראת **לפני** כל קריאת-מודל ומחזירה
`{ok:false}` כשעברו את התקרה היומית. להחזיר 429 עם הודעה ידידותית בעברית. תקרה
נפרדת ומחמירה יותר למשתמשים שנרשמו בשבוע האחרון.

**נקודות שצריך לעטוף:** `/api/chat`, `/api/teach`, `/api/tutor`, `/api/see`,
`/api/stt`, `/api/tts`, `/api/tts-stream`, `/api/problem`, `/api/parent/verdict`,
`/api/parent/topic`.

---

## S-06 · הרשמה חופשית בלי הגבלה — מפעל חשבונות

**קובץ:** `server.js` (ענף `/api/register`)
**זמן:** יום

**מה שבור.** אין הגבלת-קצב, אין אימות-מייל, אין הזמנה. סקריפט פותח אלף חשבונות בדקה,
וכל אחד מהם מקבל תקציב-מודל מלא (כי S-05 לא אוכף).

**מה לעשות.** להגביל הרשמות לפי IP (למשל 3 ליום) דרך אותו `lib/throttle.js` של S-01.
לשקול קוד-הזמנה או אימות-מייל, במיוחד לאור S-13 (הסכמת הורה).

---

## S-07 · הילד קורא את חוות-הדעת הפסיכולוגית שנכתבה עליו

**קבצים:** `lib/parent-auth.js`, `server.js` (ענפי `/api/parent/*`)
**זמן:** יום

**מה שבור.** אזור ההורים מתחבר עם **שם-המשתמש והסיסמה של הילד עצמו**. אין פרטי-הורה
נפרדים. ילד בן 10 שיודע את הסיסמה שלו — כלומר כל ילד — נכנס ל-`/parent` וקורא מה
"הפסיכולוג" ו"המורה" כתבו עליו: קשיים, דפוסי-התנהגות, הערכות רגשיות.

זה גם אומר שאין דרך לשלול גישת-הורה בלי לשנות את סיסמת הילד.

**מה לעשות.** אחת משתיים:
- **עדיף:** פרטי-הורה נפרדים. בהרשמה מבקשים מייל-הורה, שולחים אליו קישור-הגדרה, וההורה
  קובע סיסמה משלו. `lib/parent-auth.js` מאמת מולה.
- **מינימלי:** קוד-הורה נפרד (4–6 ספרות) שנקבע בהרשמה ונדרש בנוסף לסיסמת הילד.

בכל מקרה — לשקול אם חוות-דעת פסיכולוגית צריכה בכלל להיות נגישה, ואם כן למי.

---

## S-08 · `/api/method-ok` — ילד אחד מאשר שיטת-לימוד לכל הילדים

**קובץ:** `server.js` (ענף `/api/method-ok`)
**זמן:** שעות

**מה שבור.** הענף מאשר שיטת-לימוד, וההשפעה שלו **גלובלית** — היא חלה על כל התלמידים
במערכת. הבדיקה היחידה היא שיש session של תלמיד כלשהו. כלומר כל ילד יכול לאשר או לפסול
שיטות-לימוד לכולם.

**מה לעשות.** להעביר את הענף לשער האדמין (`asid`), כמו שאר `/api/admin/*`. אם תלמיד
אמור להשפיע — לשמור את ההעדפה **בפרופיל שלו בלבד** ולא בקובץ המשותף.

---

## S-09 · `/api/chat` מנפח קובץ-זיכרון משותף לכל הילדים

**קובץ:** `agent/orchestrator.js`
**זמן:** שעות

**מה שבור.** כתיבה לקובץ-זיכרון אחד שמשותף לכל המשתמשים, בלי תקרה. תלמיד יחיד יכול
לנפח אותו עד שהקריאה שלו מאטה את המערכת לכולם, או עד שהוא עובר את תקרת ה-950KB של
Upstash (ראו S-15) ואז **כל הזיכרון נמחק**.

**מה לעשות.** להפריד את הזיכרון לקובץ-לכל-משתמש, או לתחום כל משתמש למכסת-רשומות. לחתוך
רשומות ישנות בגישת חלון-מתגלגל.

---

## S-10 · `/api/problem` עם נושא שאינו בתכנית — קריאת-AI בתשלום וקובץ חדש בכל בקשה

**קובץ:** `agent/mathematician-agent.js`
**זמן:** שעות

**מה שבור.** `body.topic` מגיע מהלקוח. נושא שלא קיים בתכנית-הלימודים מפעיל יצירת-שאלות
במודל **וכותב קובץ חדש**. בקשות חוזרות עם נושאים אקראיים = הוצאה בלתי-מוגבלת + הצפת
תיקיית הנתונים.

> שים לב: `userId` ו-`grade` בענף הזה **כן** נלקחים מה-session ומהחשבון — זה תקין ולא
> צריך לשנות. הבעיה היא רק ב-`topic`.

**מה לעשות.** לאמת ש-`body.topic` הוא מפתח-עלה קיים בתכנית של הכיתה שלו לפני כל
קריאת-מודל. אם לא — להחזיר 400 בלי לפנות למודל ובלי לכתוב קובץ.

---

## S-11 · `/api/tts` — טקסט בלי תקרת-אורך

**קובץ:** `server.js` (ענף `/api/tts`)
**זמן:** דקות

**מה שבור.** `body.text` נשלח להקראה בלי הגבלת-אורך. החיוב אצל ספק ההקראה הוא **לפי
תו**. בקשה אחת עם מגה-בייט טקסט היא חשבונית.

**מה לעשות.** `String(body.text || "").slice(0, 1200)` — משפט-מורה אמיתי לעולם לא ארוך
מזה. להחזיר 400 אם המקור ארוך משמעותית, כדי שהבעיה תהיה גלויה ולא שקטה.

**בדיקה:** לשלוח 100KB טקסט ולוודא 400 או חיתוך.

---

## S-12 · `/api/stt` — 12MB אודיו, ומסלול-גיבוי שמחייב שני ספקים

**קובץ:** `server.js` (ענף `/api/stt`)
**זמן:** שעות

**מה שבור.** תקרת-הגוף היא 12MB. בנוסף, מסלול-הגיבוי מנסה ספק שני כשהראשון נכשל —
כלומר בקשה כושלת יכולה לחייב **פעמיים**.

**מה לעשות.** להוריד את התקרה ל-~2MB (מספיק ל-60 שניות של דיבור ב-16kHz מונו). להגביל
מספר בקשות-תמלול לדקה לכל משתמש. לוודא שמסלול-הגיבוי לא רץ על שגיאות-קלט (400) אלא רק
על תקלת-ספק אמיתית.

---

## S-13 · ילד בן 6 נרשם לבד עם בית-ספר ומייל, בלי הסכמת הורה

**קובץ:** `lib/users.js` (טופס ההרשמה)
**זמן:** יום

**מה שבור.** טופס ההרשמה אוסף שם, **גיל**, כיתה, **בית-ספר**, מגדר ומייל — מילד, בלי
שום מנגנון הסכמת-הורה. זה תיק מזהה על קטין. במקומות רבים בעולם זו גם חשיפה רגולטורית
(COPPA/GDPR-K), אבל גם בלי הרגולציה זה פשוט מידע שאסור לאסוף ככה.

**מה לעשות.**
- להפוך את **בית-הספר לשדה רשות**, או להסיר אותו. הוא לא נחוץ לתפקוד המערכת.
- לדרוש מייל-הורה בהרשמה ולשלוח אליו הודעת-אישור לפני הפעלת החשבון.
- להוסיף מסך פרטיות קצר בעברית פשוטה: מה נאסף, לאן זה נשלח (כולל **S-16** — השם נשלח
  למודל), ואיך מוחקים.

---

## S-14 · מחיקת חשבון לא מוחקת מ-Upstash — הנתונים חוזרים לחיים

**קובץ:** `lib/cloud-store.js`
**זמן:** שעות

**מה שבור.** מחיקת משתמש מנקה את הקבצים המקומיים, אבל אין מסלול-מחיקה מקביל ב-Upstash.
ה-deploy הבא של Render מושך את המצב מהענן — **והילד המחוק חוזר**, עם כל הנתונים שלו.

**מה לעשות.** להוסיף `remove(key)` ב-`lib/cloud-store.js` ולקרוא לה מכל מסלול-מחיקה.
לכתוב פונקציית `deleteUserCompletely(userId)` אחת שמוחקת: `users.json`, `progress/`,
`profiles/`, `assessments/`, `memory/*`, `sessions`, `parent-sessions`, `token-usage`
— **וגם את המקבילות ב-Upstash**.

**בדיקה:** למחוק חשבון-בדיקה, להפעיל מחדש את השרת, ולוודא שהוא לא חוזר.

---

## S-15 · תקרת 950KB שקטה — כל היסטוריית הלמידה נמחקת ב-deploy הבא

**קובץ:** `lib/cloud-store.js`
**זמן:** יום

**מה שבור.** יש תקרת-גודל לשכפול ל-Upstash. כשקובץ עובר אותה — **הוא פשוט לא משוכפל,
בשקט**. ב-Render בתוכנית החינמית אין דיסק קבוע, כך שה-deploy הבא מוחק את המקומי ומושך
מהענן את הגרסה הישנה. התוצאה: אובדן שקט של היסטוריית-למידה של ילדים.

**מה לעשות.** לפצל את הקבצים הגדולים (זיכרון לכל משתמש בנפרד — ראו S-09), **ולזעוק
בלוג** כשקובץ עובר את התקרה במקום לוותר בשקט. לשקול דיסק-קבוע ב-Render או מסד-נתונים
אמיתי.

---

# בינוני

## S-16 · השם ובית-הספר של הילד נשלחים למודל בלי שאף אחד יודע

**קובץ:** `lib/assessments.js`
**זמן:** שעות

השם האמיתי של הילד (ולעיתים בית-הספר) נכנס ל-prompt שנשלח ל-Anthropic. זה לא מתועד
בשום מקום שההורה רואה.

**מה לעשות.** להחליף את השם בכינוי גנרי ("התלמיד/ה") לפני שליחה — הערכה איכותית לא
צריכה שם אמיתי. אם השם נחוץ לניסוח, לתעד את זה במסך-הפרטיות של S-13.

---

## S-17 · איפוס-סיסמה ומחיקת-חשבון לא מבטלים sessions קיימים

**קבצים:** `lib/users.js`, `server.js` (ענפי `/api/admin/user/*`)
**זמן:** דקות

הבעלים משנה לילד סיסמה — ומי שכבר מחובר עם העוגייה הישנה **נשאר מחובר**. אותו דבר
במחיקת-חשבון: ה-session ממשיך לעבוד.

**מה לעשות.** בכל שינוי-סיסמה ובכל מחיקה, למחוק את כל ה-sessions של אותו `userId`
בשלושת הקבצים (`sessions`, `parent-sessions`, ואם רלוונטי `admin-sessions`).

---

## S-18 · עוגיות בלי `Secure`, ואפס כותרות-אבטחה

**קבצים:** `lib/session.js`, `lib/admin-auth.js`, `lib/parent-auth.js`, `server.js`
**זמן:** דקות

העוגיות מוגדרות `HttpOnly; Path=/; SameSite=Lax` — **בלי `Secure`**. אין גם שום
כותרת-אבטחה בתגובות: אין CSP, אין `X-Content-Type-Options`, אין `X-Frame-Options`.
האתר ניתן להטמעה ב-iframe.

**מה לעשות.**
1. להוסיף `Secure` לשלוש העוגיות כשהחיבור הוא https (ב-Render תמיד):
   `const secure = process.env.NODE_ENV === "production" ? "; Secure" : "";`
2. להוסיף בפונקציית `send` ב-`server.js` את הכותרות:
   ```
   X-Content-Type-Options: nosniff
   X-Frame-Options: DENY
   Referrer-Policy: same-origin
   Content-Security-Policy: default-src 'self'; img-src 'self' data:;
     style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
     font-src 'self' https://fonts.gstatic.com; connect-src 'self'
   ```
   **זהירות:** ה-CSP חייב להתאים למה שהאתר באמת טוען. לבדוק כל דף אחרי ההוספה —
   `learn-board.html` מריץ קוד שהמודל מייצר (ראו S-20) ועלול להיחסם.

---

## S-19 · בקשות GET שמפעילות מודלים — חשופות ל-CSRF

**קובץ:** `server.js` (ענפי `/api/parent/verdict`, `/api/parent/topic`)
**זמן:** שעות

הענפים האלה מגיבים ל-**GET**, מפעילים שלוש קריאות-מודל, ומאומתים בעוגייה בלבד. דף
צד-שלישי שההורה פותח יכול להפעיל אותם דרך `<img src="...">` ולשרוף את התקציב.
בנוסף `?refresh=1` **עוקף את מנעול ה-inflight** ומאפשר יצירה מקבילה חוזרת.

**מה לעשות.** להעביר פעולות יקרות ל-POST, לבדוק את כותרת `Origin`, ולהחיל את מנעול
ה-inflight גם על `refresh=1`. להגביל `refresh` לפעם ב-10 דקות לכל ילד.

---

## S-20 · קוד JS שהמודל כותב רץ בדפדפן של הילד, וה-prompt נשלט מה-URL

**קובץ:** `learn-board.html`
**זמן:** שעות

הלוח מריץ קוד שהמודל מייצר. חלק מה-prompt מגיע מפרמטרים ב-URL. כלומר קישור מעוצב
יכול לגרום למודל לייצר קוד שרץ בדפדפן של הילד — XSS דרך המודל.

**מה לעשות.** לא להריץ קוד חופשי מהמודל. להחליף בממשק-כלים מוגבל: המודל מחזיר **תיאור
מובנה** (JSON) של מה לצייר, וקוד קבוע ובדוק מבצע אותו. לוודא שכל פרמטר מה-URL מנוקה
לפני שהוא נכנס ל-prompt.

---

## S-21 · `/api/see` — 14MB לזיכרון בכל בקשה

**קובץ:** `server.js` (ענף `/api/see`)
**זמן:** דקות

תקרת-גוף של 14MB לתמונה, בלי הגבלת-בקשות. כמה בקשות במקביל מספיקות למצות את הזיכרון
של מכונת Render חינמית, וכל אחת גם עולה כסף במודל-הראייה.

**מה לעשות.** להוריד ל-~3MB, ולהגביל בקשות-ראייה לדקה לכל משתמש (דרך S-05).

---

## S-22 · `/api/tts-stream` לעולם לא נקרא — והמנגנון היחיד שחוסם עלות מת

**קובץ:** `server.js`
**זמן:** דקות

הענף `/api/tts` נבדק **לפני** `/api/tts-stream` עם `startsWith`, ולכן בולע אותו —
`/api/tts-stream` מתחיל ב-`/api/tts`. מנגנון ה-barge-in (עצירת ההקראה כשהילד מדבר),
שהוא החוסם-עלות היחיד בקוד, לעולם לא רץ.

**מה לעשות.** לבדוק את `/api/tts-stream` **לפני** `/api/tts`, או להשוות נתיב מדויק
במקום `startsWith`. **זה באג תפקודי ולא רק אבטחתי** — שווה לתקן בכל מקרה.

---

## S-23 · אורקל-תזמון חושף אילו שמות-משתמש קיימים

**קובץ:** `lib/users.js`
**זמן:** דקות

שם-משתמש שלא קיים מחזיר תשובה תוך ~0.4ms; שם קיים עם סיסמה שגויה לוקח ~24ms (זמן
ה-scrypt). ההפרש מאפשר למפות אילו ילדים רשומים.

**מה לעשות.** להריץ scrypt-דמה גם כשהמשתמש לא נמצא, כדי שהזמן יהיה אחיד.

---

# נמוך

## S-24 · מונה-כישלונות גלובלי נועל את הבעלים מחוץ ל-`/admin`

**קובץ:** `lib/admin-auth.js` — 6 ניסיונות שגויים מכל מקור בעולם נועלים את הכניסה
לבעלים עצמו. **מה לעשות:** למפתח את המונה לפי IP במקום גלובלי.

## S-25 · שמות-משתמש כפולים דרך ניקוד בלתי-נראה ומרוץ-כתיבה

**קובץ:** `lib/users.js` — אין נרמול Unicode, ואין נעילה בין הבדיקה לכתיבה.
**מה לעשות:** `String.prototype.normalize("NFKC")`, הסרת תווי-רוחב-אפס, ובדיקה-וכתיבה
אטומית.

## S-26 · URL עם אחוז שבור מפיל 500 וכותב stack-trace — בלי התחברות

**קובץ:** `server.js` — `decodeURIComponent` על URL פגום זורק.
**מה לעשות:** לעטוף ב-try/catch ולהחזיר 400.

## S-27 · נושא בשם `__proto__` נמחק בשקט מהפרופיל

**קובץ:** `lib/learner-profile.js` — **מה לעשות:** `Object.create(null)` למפות-נושאים,
או דחיית מפתחות `__proto__`/`constructor`/`prototype`.

## S-28 · `/api/status` פתוח לעולם ומדליף תצורת-ענן

**קובץ:** `server.js` — מחזיר ספק-מודל, שם-פרויקט GCP, מודל-הקול והאם Upstash מוגדר,
בלי התחברות. **מה לעשות:** לדרוש session, או לצמצם לתשובת `{ok:true}` בלבד ולהשאיר את
האבחון מאחורי שער האדמין.

## S-29 · שגיאות Google/Vertex מוחזרות מילה-במילה לדפדפן

**קובץ:** `server.js` — הודעות-שגיאה של הספק כוללות מזהי-פרויקט ונתיבי-API.
**מה לעשות:** לרשום בלוג את המלא, ולהחזיר ללקוח הודעה גנרית בעברית.

## S-30 · Google Fonts כצד-שלישי בכל דף של ילדים

**קובץ:** `home.html` ושאר הדפים — טעינה מ-CDN חיצוני בלי SRI ובלי CSP, וכל בקשה
חושפת ל-Google את ה-IP של הילד. **מה לעשות:** להוריד את הפונטים ולהגיש אותם מ-
`/fonts/` — כבר עושים את זה עבור Arca Morphe ו-Google Sans, אז התשתית קיימת.

---

## סדר עבודה מומלץ

**עכשיו, לפני הכול (תפעולי, לא קוד):**
1. לבטל את מפתח Anthropic הישן בקונסולה — **S-02**
2. למחוק את שלושת קובצי ה-sessions — הפעולה בתוך **S-03**

**גל ראשון — עוצר נזק (יום עבודה):**
S-01 (הגבלת-קצב) ← S-06 (הרשמה) ← S-05 (תקציב) ← S-11, S-12, S-21 (תקרות) ← S-22 (הבאג)

**גל שני — מצמצם נזק אם משהו כן ידלוף:**
S-03 (גיבוב טוקנים) ← S-17 (ביטול sessions) ← S-18 (עוגיות וכותרות) ← S-14 (מחיקה מלאה)

**גל שלישי — מבני, דורש החלטות מוצר:**
S-07 (חשבון הורה), S-13 (הסכמה ופרטיות), S-20 (קוד מהמודל), S-15 (persistence)

**גל רביעי — ניקיון:** S-08, S-09, S-10, S-16, S-19, S-23 ואילך.

---

## בדיקת-שפיות אחרי כל משימה

```bash
node --check server.js && node --check lib/<הקובץ ששונה>.js
node server.js &                       # השרת חייב לעלות בלי שגיאה
sleep 5
for p in /auth /home /practice /learn /progress /parent /admin; do
  curl -s -o /dev/null -w "$p %{http_code}\n" "http://localhost:8787$p"
done
```
כל הדפים חייבים להחזיר 200 או 302 — **לא 500**.
