.env
node_modules/

# כל data/ נשאר פרטי (חשבונות, sessions, זיכרון) — חוץ ממאגר השאלות המשותף
/data/*
!/data/bank/
.design-backup/
.DS_Store

# הגדרות מקומיות של Claude Code (לא חלק מהאפליקציה)
.claude/settings.local.json
# ארכיון הפונט (כפול — הקבצים כבר ב-fonts/)
arca-morphe.zip

# קבצים מקומיים שלא שייכים לענן
Google_Sans.zip
backup/
.claude/launch.json
