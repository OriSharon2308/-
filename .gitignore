.env
node_modules/

# כל data/ נשאר פרטי (חשבונות, sessions, זיכרון) — חוץ מתוכן משותף:
# בנק שאלות, שיטות-לימוד שאושרו, וחומרי-עזר — אלה חלק מהמערכת ועולים לענן
/data/*
!/data/bank/
!/data/methods/
!/data/reference/
.design-backup/
.DS_Store

# הגדרות מקומיות של Claude Code (לא חלק מהאפליקציה)
.claude/settings.local.json
# ארכיון הפונט (כפול — הקבצים כבר ב-fonts/)
arca-morphe.zip

# קבצים מקומיים שלא שייכים לענן
Google_Sans.zip
backup/
.claude/launch.json
